<?php
// ============================================================
// CONFIGURAÇÕES LOCAIS — copie para config.local.php e preencha
// config.local.php está no .gitignore e nunca deve ser commitado
// ============================================================
return [
    'DB_HOST'        => 'localhost',
    'DB_NAME'        => 'seu_banco',
    'DB_USER'        => 'seu_usuario',
    'DB_PASS'        => 'sua_senha_aqui',

    'APP_URL'        => 'https://helpti.seudominio.com.br',
    'APP_EMAIL_FROM' => 'HelpTI <noreply@seudominio.com.br>',
    'APP_EMAIL_REPLY'=> 'noreply@seudominio.com.br',

    'DEBUG_MODE'     => false,

    // Opcional — habilita "Classificar com IA" no portal público
    'GEMINI_API_KEY' => '',

    // Token para o endpoint de monitoramento health.php  (gere: openssl rand -hex 24)
    'HEALTH_TOKEN'   => '',

    // Desligar/reiniciar estações Windows pela rede (Ferramentas de TI).
    // Requer o pacote `samba-common-bin` (comando `net`) no servidor e uma conta
    // de administrador local/de domínio das estações. Deixe em branco p/ desabilitar.
    // Nas estações: firewall liberando "Instrumentação de Gerenciamento do Windows (WMI)"
    // ou compartilhamento admin (RPC/445), na rede local.
    'SHUTDOWN_USER'   => '',        // ex: 'administrador'
    'SHUTDOWN_PASS'   => '',
    'SHUTDOWN_DOMAIN' => '',        // ex: 'CLINICA'  (vazio = conta local da estação)
];
